<% content_for :title, "Hold Confirmed!" %>

<div class="max-w-md mx-auto mt-10 text-center">
  <div class="bg-white border border-gray-200 rounded-2xl p-10 shadow-sm">
    <div class="text-5xl mb-4 text-card">✓</div>
    <h1 class="text-2xl font-semibold mb-2">Hold confirmed!</h1>
    <p class="text-gray-500 text-sm">
      You're all set. Your hold is fully refunded when you show up.
    </p>
    <% if @booking_link %>
      <p class="mt-4 text-sm font-medium"><%= @booking_link.service_name %></p>
    <% end %>
  </div>
</div>
